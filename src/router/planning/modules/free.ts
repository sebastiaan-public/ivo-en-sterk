import { RawLocation } from 'vue-router'


export function nextRoute(): RawLocation | null {
  
  // const isLoggedIn = store.getters['auth/loggedIn']

  // if (!isLoggedIn) {
  //   return { name: 'free' }
  // }
  return null
}
